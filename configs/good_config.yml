version: "2.2"

server:
  host: "localhost"
  port: 443

auth:
  password: "An0th3r!Str0ng#Pass"

log:
  output: "/var/log/service.log"
  level: "info"

tls:
  enabled: true

storage:
  digest-algorithm: "sha256"
